# Dictionaries

user = {
    "username": "kelvin",
    "email": "kelvin@example.com",
    "age": 25,
    "is_active": True
}
print(user)

# Access values
print(user["username"])
print(user["email"])

# Add data
user["country"] = "Kenya"

print(user)

# Change data
user["age"] = 26

print(user)